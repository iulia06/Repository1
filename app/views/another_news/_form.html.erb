<%= form_with(model: another_news, local: true) do |form| %>
  <% if another_news.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(another_news.errors.count, "error") %> prohibited this another_news from being saved:</h2>

      <ul>
      <% another_news.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
